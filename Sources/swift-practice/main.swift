// Sources/swift-practice/main.swift

import Foundation
import Note  // Import the 'Note' module to use functions from 'functions.swift'

print(greet(person: "Bill", from: "Cupertino"))

// Example usage
let number = 5
let result = factorial(of: number)

print("The factorial of \(number) is \(result).")